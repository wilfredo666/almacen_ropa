---ftp---
servidor ftp: 45.87.81.14
usuario: u248590315
password: Admin123!
puerto: 21
path:/public_html

---hostinger----
modafashion.bo@gmail.com
Jorge123%$

accedo bd de hostinger:
usuario: u248590315_almacen
bd: u248590315_almacen_ropa
password: Admin321!

almacen_ropa
------------
acceso a sistema
usuario: admin
pass: password
categoria: administrador

almacen_ropa (host externo)
---------------------------
-vendedor
seller
password
-administrador
admin
password

Base de datos:
mysql -h 45.87.81.0 -u u248590315_almacen -p Admin321!
o
https://auth-db365.hostinger.com/index.php?db=u248590315_almacen_ropa
u248590315_almacen 
Admin321!

***** a resolver*****
no se puede eliminar productos si esta relacionado con:
-registro de traspasos a tienda
-registro de ingresos
-quitar del navbar del el icono para personalizar el mismo y cambiarlo por la expancion de pantalla full

no se puede eliminar usuarios si esta relacionado con:
-tienda

-debe elejirse si o si un usuario de tipo cajero al crear tienda
-restringir la cantida que se puede traspasar a almacen en base al stock del almacen
-hacer que el stock del almacen vuelva a aumentar si se quitan el traspaso a otro almacen
-no se pueden eliminar los traspasos a las tiendas
-poner "registro de ingresos en la vista listaIngresos.php"
-poner "stock - (almacen) en la vista stockAlmacen.php"
*****
-restrict: que no se puede borrar o catualiza un registro si este esta referenciado en otra tabla (la tabla con la llave foreana), pero si no lo esta, entonces si se podra borrar o actualizar
-cascade: que se borrara o actualizar todos los registros de la tabla dependiente (la tabla con la llave foreana) que contengan el valor de una clave primaria en la tabla referenciada
-set null: Establece a NULL el valor de la clave secundaria (la tabla con la llave foreana) cuando se elimina o actualiza el registro en la tabla principal
-no action: En MySQL funciona igual que RESTRICT.

(post.-) las llaves referenciadas son las primarias

/////// al subir en la nube /////
cambiar a https en "panel_control.php" y "panel_control_tienda.php"
cambiar la conexion a la bd en "validar.php" y "conexion.php"

---git---
https://github.com/wilfredo666/almacen_ropa.git


consultas:
-modo 1 para unir datos:
SELECT desc_almacen, descripcion, talla, stock FROM stock_almacen st, producto pt, almacen al WHERE pt.id_producto=st.id_producto and al.id_almacen=st.id_almacen AND al.id_almacen=4

**************
cuando no inicia en hosting con la vista adecuada css, entonces quiza es por al peticion http --> htpps
**************

--------***----

/* con esto extraemos todos los datos de todos los productos correspondientes a solo un almacen en concreto*/
 $res=mysqli_query($conectador,"SELECT id_producto, descripcion, categoria, talla, precio, color FROM producto
                        WHERE id_producto
                        in (SELECT DISTINCT id_producto FROM `ingreso_almacen` WHERE `id_almacen`=$id_almacen) ");

/*obtener datos relacionados*/
forma 1)
SELECT descripcion, categoria, talla, precio, stock
FROM producto pt, stock_almacen sta
WHERE pt.id_producto=2 AND sta.id_almacen=2

forma 2)
SELECT producto.id_producto, descripcion, categoria, talla, precio, stock
FROM stock_almacen
JOIN producto
ON producto.id_producto=stock_almacen.id_producto
WHERE id_almacen=2

forma 3)
SELECT *
FROM stock_almacen
JOIN producto
ON producto.id_producto=stock_almacen.id_producto
WHERE id_almacen=2

                    
/*agregar item a lista de ventas - lado derecho en "FormRegVenta" */
var lista=[];
function AgregarCarrito(){
    var id_stock=$("#id_stock").val();
    lista.push(id_stock);
    var obj={lista:lista};
    $.ajax(
        {
            type:"POST",
            url:host+"AgregarCarrito.php",
            data:obj,
            success:function(data){
                $("#carrito").html(data);
               /* tambien pueden funcionar con .append y enviando solo el id y no un arreglo con los id*/
            }
        }
    )
}

